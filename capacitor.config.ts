import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'TDS200_h22_1101',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
